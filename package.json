{
  "name": "vanderven-sys",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply vanderven-crm --local",
    "db:migrate": "wrangler d1 migrations apply vanderven-crm --remote"
  },
  "devDependencies": {
    "wrangler": "^4.24.0"
  }
}
